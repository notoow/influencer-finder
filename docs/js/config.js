/**
 * Notoow Influencer Finder - Central Configuration (SSOT)
 * 
 * Manages environment credentials and app defaults.
 */

export const CONFIG = {
  SUPABASE_URL: 'https://wqymwtvktdhofzegjawn.supabase.co',
  SUPABASE_ANON_KEY: 'YOUR_SUPABASE_ANON_KEY', // Fill with anon key from Supabase Dashboard
  DEFAULT_PLATFORM: 'instagram',
  DEFAULT_COUNTRY: 'ALL',
  RESULT_LIMIT: 50
};
